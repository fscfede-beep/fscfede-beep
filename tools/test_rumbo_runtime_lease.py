from __future__ import annotations\n\nimport time\nfrom pathlib import Path\n\nfrom tools.rumbo_runtime_lease import checkpoint, finish, load, start\n\ndef test_runtime_lease_preserves_start_and_fails_short_duration(tmp_path: Path) -> None:\n    path = tmp_path / 'lease.json'\n    lease = start(path, 1.0)\n    assert load(path).lease_id == lease.lease_id\n    time.sleep(0.01)\n    receipt = finish(path, 0)\n    assert receipt.command_pass is True\n    assert receipt.duration_pass is False\n    assert receipt.overall_pass is False\n\ndef test_runtime_lease_checkpoint_reports_observed_duration(tmp_path: Path, capsys) -> None:\n    path = tmp_path / 'lease.json'\n    start(path, 0.0)\n    checkpoint(path)\n    output = capsys.readouterr().out\n    assert 'duration_pass' in output\n    assert 'lease_id' in output